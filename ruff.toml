[lint.per-file-ignores]
# the test harness and render scripts must extend sys.path (and register
# the addon) before importing from it, so imports cannot all sit at the
# top of the file
"test/smoke_test.py" = ["E402"]
"docs/render_trajectory_gif.py" = ["E402"]
